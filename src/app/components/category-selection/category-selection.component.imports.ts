
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';

const common = [CommonModule];
const module = [FormsModule];

const categorySelectionComponentImports = [...common,...module];

export default categorySelectionComponentImports;
